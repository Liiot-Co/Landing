---
import LiiotMark from "@/components/ui/LiiotMark.astro";
import { LIIOT_CALENDLY_URL } from "@/constants/contact";

interface NavLink {
  name: string;
  path: string;
}

const navLinks: NavLink[] = [
  { name: "Por qué existimos", path: "#por-que-existimos" },
  { name: "Cómo trabajamos", path: "#como-trabajamos" },
  { name: "Proyectos", path: "#proyectos" },
  { name: "Equipo", path: "#equipo" },
  { name: "Agendar", path: "#agendar" },
];
---

<nav
  id="site-nav"
  class="fixed top-0 left-0 right-0 z-50 mix-blend-difference"
  role="navigation"
  aria-label="Navegación principal"
>
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex h-20 items-center justify-between sm:h-24">
      {/* Brand */}
      <a href="#hero" class="group flex items-center gap-2.5" aria-label="Liiot — Inicio">
        <LiiotMark class="size-7 sm:size-8" color="white" />
        <span class="text-lg font-bold tracking-tight text-white">Liiot</span>
      </a>

      {/* Hamburger — única entrada de navegación visible */}
      <button
        id="nav-toggle"
        class="relative flex size-11 flex-col items-center justify-center gap-[7px]"
        aria-label="Abrir menú"
        aria-expanded="false"
        aria-controls="nav-drawer"
      >
        <span
          id="nav-line-1"
          class="block h-[1.5px] w-6 origin-center bg-white transition-transform duration-300 ease-out"
        ></span>
        <span
          id="nav-line-2"
          class="block h-[1.5px] w-6 origin-center bg-white transition-transform duration-300 ease-out"
        ></span>
      </button>
    </div>
  </div>
</nav>

{/* Overlay drawer — fondo sólido propio (fuera del blend-difference del nav) */}
<div
  id="nav-drawer"
  class="fixed inset-0 z-40 flex translate-y-[-8px] flex-col justify-center bg-background opacity-0 pointer-events-none transition-all duration-500 ease-[cubic-bezier(0.22,1,0.36,1)]"
  role="menu"
  aria-hidden="true"
>
  <div class="container mx-auto px-4 sm:px-6 lg:px-8">
    <ul class="flex flex-col gap-2">
      {
        navLinks.map((link, i) => (
          <li>
            <a
              href={link.path}
              data-nav-item
              role="menuitem"
              class="drawer-link block py-2 text-[12vw] font-bold leading-[1.05] tracking-tight text-foreground transition-colors duration-300 hover:text-accent sm:text-6xl lg:text-7xl"
              style={`transition-delay:${i * 40}ms`}
            >
              {link.name}
            </a>
          </li>
        ))
      }
    </ul>

    <a
      href={LIIOT_CALENDLY_URL}
      target="_blank"
      rel="noopener noreferrer"
      data-nav-item
      role="menuitem"
      class="mt-12 inline-flex items-center gap-2 text-sm font-bold uppercase tracking-[0.2em] text-accent"
    >
      Agendar consultoría
      <span aria-hidden="true">&rarr;</span>
    </a>
  </div>
</div>

<script>
  const toggle = document.getElementById("nav-toggle")!;
  const drawer = document.getElementById("nav-drawer")!;
  const line1 = document.getElementById("nav-line-1")!;
  const line2 = document.getElementById("nav-line-2")!;
  const navItems = drawer.querySelectorAll<HTMLElement>("[data-nav-item]");

  let open = false;

  function setDrawer(next: boolean) {
    open = next;
    toggle.setAttribute("aria-expanded", String(open));
    drawer.setAttribute("aria-hidden", String(!open));
    drawer.classList.toggle("opacity-0", !open);
    drawer.classList.toggle("pointer-events-none", !open);
    drawer.classList.toggle("translate-y-[-8px]", !open);
    line1.style.transform = open ? "translateY(4px) rotate(45deg)" : "none";
    line2.style.transform = open ? "translateY(-4px) rotate(-45deg)" : "none";
    document.body.style.overflow = open ? "hidden" : "";
  }

  toggle.addEventListener("click", () => setDrawer(!open));
  navItems.forEach((el) => el.addEventListener("click", () => setDrawer(false)));
</script>
